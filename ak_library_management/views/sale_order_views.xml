<odoo>

    <!--inherit sale order form view-->
    <record id="view_inherit_sale_order_add_button_form" model="ir.ui.view">
        <field name="name">sale.order.add.button.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_confirm'][2]" position="attributes">
                <attribute name="invisible">state != 'draft' or is_check</attribute>
            </xpath>
            <xpath expr="//button[@name='action_confirm'][2]" position="after">
                <button name="action_approve" string="Approve" type="object" invisible="not is_check"/>
            </xpath>
            <xpath expr="//button[@name='action_cancel']" position="attributes">
                <attribute name="invisible">state not in ['draft', 'sent', 'sale'] or not id or locked or is_check</attribute>
            </xpath>
            <xpath expr="//button[@name='action_cancel']" position="after">
                <button name="action_reject" string="Reject" type="object" invisible="not is_check"/>
            </xpath>
        </field>
    </record>
</odoo>